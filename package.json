{
    "name": "aircon-service",
    "version": "2.0.0",
    "private": true,
    "description": "Aircon service management — Express + Prisma API serving a React SPA",
    "scripts": {
        "setup": "npm install --prefix backend && npm install --prefix frontend",
        "db:up": "docker compose up -d --wait",
        "db:init": "npm run setup --prefix backend",
        "dev": "npm run dev --prefix backend",
        "dev:web": "npm run dev --prefix frontend",
        "build": "npm run build --prefix frontend && npm run build --prefix backend",
        "start": "npm start --prefix backend",
        "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend",
        "test": "npm test --prefix backend"
    }
}
